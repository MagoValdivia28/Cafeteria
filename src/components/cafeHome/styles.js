import { StyleSheet } from "react-native";

const Styles = StyleSheet.create({
    container: {
    
        alignItems: "center",
    },
    image:{
        borderRadius:20,
        margin: 15
    },

    

})

export default Styles;