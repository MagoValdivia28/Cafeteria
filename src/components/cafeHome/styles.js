import { StyleSheet } from "react-native";

const Styles = StyleSheet.create({
    container: {
        alignItems: "center",
    },
    image:{
        borderRadius:20
    }

})

export default Styles;